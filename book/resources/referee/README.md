# Referee

Purpose
- Referee responses if the book material is submitted as papers.

What goes here
- `RESPONSE_TO_REFEREE.tex` and supporting files.

Conventions
- Use the templates in `_shared/templates/` and standards in `_shared/standards/`.
- Do not mark issues as fixed unless the fix exists in the manuscript.